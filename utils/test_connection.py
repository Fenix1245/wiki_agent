

# Импортируем необходимые библиотеки
from dotenv import load_dotenv  # Для загрузки ключа из файла .env
import os                       # Для работы с операционной системой (чтение переменных окружения)

# Загружаем переменные из файла .env в окружение
load_dotenv()

# Пытаемся получить наш секретный ключ
api_key = os.getenv("GIGA_CHAT_API_KEY")

# Проверяем, удалось ли получить ключ
if api_key:
    print("✅ Ключ GigaChat успешно загружен из файла .env!")
    print(f"🔑 Длина ключа: {len(api_key)} символов.") # Просто чтобы убедиться, что он не пустой
    print("🎉 Все необходимые библиотеки установлены корректно!")
else:
    print("❌ ОШИБКА: Не удалось загрузить ключ 'GIGA_CHAT_API_KEY'.")
    print("Пожалуйста, проверьте:")
    print("  1. Существует ли файл .env в той же папке, что и этот скрипт.")
    print("  2. Правильно ли записано имя переменной в файле .env (GIGA_CHAT_API_KEY=ваш_ключ).")
    print("  3. Нет ли опечаток.")

# Простая проверка других критически важных библиотек
try:
    # Пытаемся импортировать главные библиотеки
    import langchain
    import chromadb
    print("✅ Библиотеки 'langchain' и 'chromadb' также готовы к работе!")
except ImportError as e:
    print(f"❌ Ошибка импорта библиотеки: {e}")
