# Импортируем необходимые инструменты (АКТУАЛЬНЫЙ СПОСОБ)
from langchain_community.embeddings import HuggingFaceEmbeddings
from langchain_community.vectorstores import Chroma
from langchain_community.llms import GigaChat  # Импортируем модель GigaChat для генерации ответов
from langchain.chains import RetrievalQA  # Импортируем цепь для вопроса-ответа
from langchain.prompts import PromptTemplate  # Импортируем инструмент для создания шаблонов вопросов
import os
import base64
from dotenv import load_dotenv

# Импортируем необходимые инструменты
from langchain_community.embeddings import HuggingFaceEmbeddings
from langchain_community.vectorstores import Chroma
from langchain_community.llms import GigaChat
from langchain.chains import RetrievalQA
from langchain.prompts import PromptTemplate
import os
import base64
from dotenv import load_dotenv

# ЧИТАЕМ КЛЮЧ НАПРЯМУЮ ИЗ ФАЙЛА .env
print("🔍 Читаю ключ напрямую из файла .env...")
env_path = '.env'

try:
    with open(env_path, 'r') as f:
        api_key = f.read().strip()
    print(f"✅ Ключ прочитан из файла, длина: {len(api_key)} символов")
    print(f"📄 Содержимое: {api_key}")
    
    if not api_key:
        raise ValueError("❌ Файл .env пустой")
        
except FileNotFoundError:
    raise ValueError("❌ Файл .env не найден")
except Exception as e:
    raise ValueError(f"❌ Ошибка чтения файла .env: {e}")

# У нас УЖЕ ключ в формате base64, поэтому используем его как есть
credentials_b64 = api_key

# Проверяем, что ключ загрузился
if not api_key:
    raise ValueError("❌ ОШИБКА: Не найден API-ключ GigaChat. Проверьте файл .env")

# У нас УЖЕ ключ в формате base64, поэтому используем его как есть


# -------------------- 1. НАСТРОЙКИ --------------------
# Указываем путь к нашей векторной базе данных
persist_directory = "./chroma_db"

# Выбираем ту же модель для эмбеддингов, что и при создании базы
embeddings = HuggingFaceEmbeddings(
    model_name='sentence-transformers/paraphrase-multilingual-mpnet-base-v2',
    model_kwargs={'device': 'cpu'}
)



# -------------------- 2. ЗАГРУЗКА БАЗЫ ЗНАНИЙ --------------------
print("🧠 Загружаю базу знаний...")
# Загружаем нашу векторную базу из папки
vector_db = Chroma(
    persist_directory=persist_directory,
    embedding_function=embeddings
)

# -------------------- 3. НАСТРОЙКА ПРОМТА --------------------
# Создаем шаблон (prompt), который будет объяснять GigaChat, как отвечать.
# Это очень важно! Мы говорим модели отвечать ТОЛЬКО на основе контекста из нашей базы.

template = """
Ты — полезный ИИ-ассистент. Отвечай на вопрос пользователя, используя ТОЛЬКО приведенный ниже контекст.
Если ответа нет в контексте, вежливо скажи, что не знаешь ответа. Не придумывай ничего от себя.

Контекст: {context}

Вопрос: {question}
"""

# Создаем объект шаблона на основе нашего текста
PROMPT = PromptTemplate(
    template=template, input_variables=["context", "question"]
)

# ... импорты и чтение ключа ...

# -------------------- 1. НАСТРОЙКИ --------------------
# Указываем путь к нашей векторной базе данных
persist_directory = "./chroma_db"

# Выбираем ту же модель для эмбеддингов, что и при создании базы
embeddings = HuggingFaceEmbeddings(
    model_name='sentence-transformers/paraphrase-multilingual-mpnet-base-v2',
    model_kwargs={'device': 'cpu'}
)

# НАСТРАИВАЕМ GIGACHAT (ЭТОТ БЛОК ДОЛЖЕН БЫТЬ ЗДЕСЬ!)
llm = GigaChat(
    credentials=credentials_b64,
    verify_ssl_certs=False,
    model="GigaChat-Pro", 
    scope="GIGACHAT_API_PERS"  # Используем персональный scope
)

# -------------------- 2. ЗАГРУЗКА БАЗЫ ЗНАНИЙ --------------------
print("🧠 Загружаю базу знаний...")
# Загружаем нашу векторную базу из папки
vector_db = Chroma(
    persist_directory=persist_directory,
    embedding_function=embeddings
)

# -------------------- 3. НАСТРОЙКА ПРОМТА --------------------
template = """
Ты — полезный ИА-ассистент. Отвечай на вопрос пользователя, используя ТОЛЬКО приведенный ниже контекст.
Если ответа нет в контексте, вежливо скажи, что не знаешь ответа. Не придумывай ничего от себя.

Контекст: {context}

Вопрос: {question}
"""

PROMPT = PromptTemplate(
    template=template, input_variables=["context", "question"]
)

# -------------------- 4. СОЗДАНИЕ ЦЕПОЧКИ ВОПРОС-ОТВЕТ --------------------
# ТЕПЕРЬ llm УЖЕ ОПРЕДЕЛЕН И МОЖЕТ БЫТЬ ИСПОЛЬЗОВАН
qa_chain = RetrievalQA.from_chain_type(
    llm=llm,  # Теперь llm существует
    chain_type="stuff",
    retriever=vector_db.as_retriever(search_kwargs={"k": 3}),
    chain_type_kwargs={"prompt": PROMPT},
    return_source_documents=True
)

# ... остальной код ...







# -------------------- 4. СОЗДАНИЕ ЦЕПОЧКИ ВОПРОС-ОТВЕТ --------------------
# Создаем цепь, которая объединяет всё вместе: поиск в базе + генерация ответа GigaChat
qa_chain = RetrievalQA.from_chain_type(
    llm=llm,  # Наша модель GigaChat
    chain_type="stuff",
    retriever=vector_db.as_retriever(search_kwargs={"k": 3}),  # Ищем 3 самых подходящих фрагмента
    chain_type_kwargs={"prompt": PROMPT},  # Передаем наш шаблон с инструкцией
    return_source_documents=True  # Это позволит нам увидеть, из каких источников был взят ответ
)

print("✅ Агент готов к работе!")
print("💡 Задавайте ваш вопрос. Для выхода введите 'выход'.")

# -------------------- 5. ЦИКЛ ВОПРОСОВ --------------------
while True:
    # Запрашиваем вопрос у пользователя
    question = input("\n❓ Ваш вопрос: ")
    
    # Проверяем, не хочет ли пользователь выйти
    if question.lower() in ['выход', 'exit', 'quit']:
        print("До свидания!")
        break
    
    # Задаем вопрос нашей цепи и получаем ответ
    result = qa_chain({"query": question})
    
    # Выводим ответ
    print(f"\n🤖 Ответ: {result['result']}")
    
    # Показываем источники ответа (из каких файлов и частей был взят ответ)
    print("\n📚 Источники:")
    for doc in result['source_documents']:
        # Извлекаем имя файла из метаданных документа
        source = doc.metadata.get('source', 'Неизвестный источник')
        # Показываем только имя файла, а не полный путь
        filename = source.split('/')[-1]  # Берем последнюю часть пути
        print(f"   - {filename}")

